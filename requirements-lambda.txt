# AWS Lambda compatible requirements
# Optimized for serverless deployment with size constraints

# Core ML libraries (CPU-only versions for Lambda)
tensorflow-cpu==2.16.1
numpy==1.26.4
Pillow==10.3.0

# Image processing (lightweight alternatives)
opencv-python-headless==4.9.0.80

# AWS Lambda utilities
boto3==1.34.0
botocore==1.34.0

# JSON handling
jsonschema==4.20.0

# Base64 and IO handling (built-in, but explicit for clarity)
# base64, io, json, tempfile are built-in Python modules
